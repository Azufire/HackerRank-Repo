#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    int a, b;
    const char* numstr[9] = { "one", "two", "three", "four", "five", "six", "seven", "eight", "nine" };
    cin >> a >> b;
    for(int i = a; i <= b; i++){
        if(1 <= i && i <= 9) {
            cout << numstr[i-1] << endl;
        } else if (i % 2 == 0) {
            cout << "even" << endl;
        } else {
            cout << "odd" << endl;
        }
    }
    return 0;
}
